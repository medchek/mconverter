/** LocalStorage key name to store app dimension upon closing */
export const APP_DIMENSIONS_LOCALSTORAGE_KEY = "app-dimensions";

// These dimensions follow the tauri.conf.json values

/** Value of minHeight in tauri.conf.json */
export const APP_MIN_HEIGHT = 600;

/** Value of minWidth in tauri.conf.json */
export const APP_MIN_WIDTH = 400;
